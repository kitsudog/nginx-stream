user                        nginx;
worker_processes            auto;
worker_rlimit_nofile        1047552;
worker_shutdown_timeout     240s;
{% include 'events.jinja' %}
error_log                   /var/log/nginx/error.log notice;
pid                         /var/run/nginx.pid;
http {
    {% include 'http.jinja' %}
}
stream {
    {% include 'stream.jinja' %}
}